# # СТРАНИЦА СПРАВОЧНИКИ

---

# 1. Цель
Реализовать страницу со списком справочников системы.

- Пользователи
- Клиенты
- Цвета
- Размеры
- Товары

Страница является точкой входа в модуль справочников.

---



# 2. Доступ

- доступ только для роли **admin**
- при отсутствии доступа отображение 403 страницы

---



# 3. Маршрут

/dictionaries

---



# 4. UI структура страницы

## 4.1 Общая структура

Страница должна содержать (сверху вниз внутри карточки):

- Хлебные крошки (см. п. 4.2) — название раздела в крошках: **«Справочники»**; отдельный заголовок страницы (h1) **не** выводится
- Список справочников

---

## 4.2 Хлебные крошки

- Реализуются **общим компонентом** `Breadcrumbs` (см. ТЗ `02 Глобальные компоненты / # Хлебные крошки.md`); путь **автоматически** строится по текущему URL (`/dictionaries`), без дублирования логики только на этой странице.
- **Размещение:** первый блок **внутри** стеклянной карточки (`auth-card`), **над** списком ссылок.
- **Внешний вид:** разделитель «/» с **пробелами с обеих сторон**; цвета и отступы — по глобальному ТЗ о крошках.
- **Текстовая цепочка** для маршрута `/dictionaries` (последний сегмент — текущая страница, не ссылка):

`Главная / Справочники`

---

## 4.3 Список справочников

Отображается в виде списка

Порядок:

1. Пользователи
2. Товары
3. Клиенты
4. Размеры
5. Цвета

---

### 4.4 Элемент списка

Каждый элемент должен содержать:
\- название справочника

Каждый элемент:
\- визуально кликабельный
\- имеет hover-эффект

---



# 5. Поведение

## 5.1 Переходы

При клике на элемент:

- Пользователи → /users

- Товары → /dictionaries/products
- Клиенты → /dictionaries/clients
- Размеры → /dictionaries/sizes
- Цвета → /dictionaries/colors

---

# 6. UI требования

- список справочников должен быть расположен по центру экрана

- ограничение ширины (max-width)
- адаптивность (mobile-friendly)
- выравнивание элементов по вертикали
- достаточные отступы между элементами

---

# 7. Ограничения

- не использовать таблицы
- не использовать модальные окна
- не смешивать с содержимым справочников
- страница выполняет только навигационную функцию

# 8. Безопасность

- доступ к странице только через проверку роли на backend
- frontend не является источником прав доступа

---

# 9. Проверка

## Сценарий:

1. Войти под admin
2. Перейти на /dictionaries
3. Убедиться, что отображается список справочников
4. Нажать на любой справочник
5. Убедиться, что происходит переход

---

## Проверка прав:

1. зайти под user или manager
2. открыть /dictionaries
3. ошибка 403