# cp .env.dev.example .env.dev
# Только backend-контейнер (Postgres в Docker @db).
DATABASE_URL=postgresql://postgres:postgres@db:5432/app
# Версия для GET /version: по умолчанию из git describe (см. README); при деплое задаётся APP_VERSION.
# Переопределение вручную: APP_VERSION=v1.0.2
# APP_ENV=dev
