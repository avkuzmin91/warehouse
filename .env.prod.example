# Скопируйте в .env.prod на сервере; файл .env.prod в git не коммитится.
# Замените учётные данные на реальные (секреты только на хосте / в secret store).
DATABASE_URL=postgresql://postgres:CHANGEME@db:5432/app
VITE_API_BASE_URL=/api
# Версия и окружение для GET /version (футер). docker-compose.prod.yml задаёт APP_ENV=prod.
# APP_ENV=prod
# APP_VERSION=1.0.1
