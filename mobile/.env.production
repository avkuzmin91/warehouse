# Нативная сборка (Android) ходит по абсолютному URL backend.
# nginx на pack-men.ru снимает префикс /api — база включает /api (см. http.ts: fetch(`${API_BASE_URL}${path}`)).
VITE_API_BASE_URL=https://pack-men.ru/api
